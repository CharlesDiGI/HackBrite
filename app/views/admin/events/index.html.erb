<p>Voici la liste des events à valider</p>

<table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Title</th>
      <th scope="col">Creator</th>
      <th scope="col">Action</th>
    </tr>
  </thead>
  <tbody>
    <% @events.each do |event| %>
        <% if pending(event) %>
    <tr>
      <th scope="row"><%= event.id %></th>
      <td><%= event.title %></td>
      <td><%= event.creator.first_name %> <%= event.creator.last_name %></td>
      <td><%= link_to "Review event", edit_admin_event_path(event)  %>
    </tr>
         <% end %>
    <% end %>
  </tbody>
</table>

<%= link_to 'Back', admin_root_path, class: "btn btn-outline-dark" %>